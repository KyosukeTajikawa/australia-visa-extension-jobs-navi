<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CORS Paths
    |--------------------------------------------------------------------------
    | CORS を有効にするパス。Sanctum の CSRF エンドポイントも忘れずに。
    */
    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Methods / Headers
    |--------------------------------------------------------------------------
    */
    'allowed_methods' => ['*'],
    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    | Cookie を伴う CORS ではワイルドカード(*)は使えません。
    | フロント（Vite）のオリジンを “完全なURL” で列挙します。
    */
    'allowed_origins' => [
        env('FRONTEND_URL', 'http://localhost:5173'),
        // 必要なら 127.0.0.1 も追加（使うときだけアンコメント）
        // 'http://127.0.0.1:5173',
    ],

    'allowed_origins_patterns' => [],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers / Max Age
    |--------------------------------------------------------------------------
    */
    'exposed_headers' => [],
    'max_age' => 0,

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    | Cookie を送受信するため true にします。
    */
    'supports_credentials' => true,
];
